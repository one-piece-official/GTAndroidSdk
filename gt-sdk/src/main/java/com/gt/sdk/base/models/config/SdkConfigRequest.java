// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: gt_ssp_config.proto
package com.gt.sdk.base.models.config;

import android.os.Parcelable;

import com.czhj.wire.AndroidMessage;
import com.czhj.wire.FieldEncoding;
import com.czhj.wire.Message;
import com.czhj.wire.ProtoAdapter;
import com.czhj.wire.ProtoReader;
import com.czhj.wire.ProtoWriter;
import com.czhj.wire.WireField;
import com.czhj.wire.internal.Internal;
import com.czhj.wire.okio.ByteString;
import com.gt.sdk.base.models.rtb.App;
import com.gt.sdk.base.models.rtb.Device;
import com.gt.sdk.base.models.rtb.User;

import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;

public final class SdkConfigRequest extends AndroidMessage<SdkConfigRequest, SdkConfigRequest.Builder> {

    public static final ProtoAdapter<SdkConfigRequest> ADAPTER = new ProtoAdapter_SdkConfigRequest();

    public static final Parcelable.Creator<SdkConfigRequest> CREATOR = AndroidMessage.newCreator(ADAPTER);

    private static final long serialVersionUID = 0L;

    /**
     * 必填！应用信息
     */
    @WireField(tag = 1, adapter = "com.gt.sdk.base.models.rtb.App#ADAPTER")
    public final App app;

    /**
     * 必填！设备信息
     */
    @WireField(tag = 2, adapter = "com.gt.sdk.base.models.rtb.Device#ADAPTER")
    public final Device device;

    /**
     * 用户信息
     */
    @WireField(tag = 5, adapter = "com.gt.sdk.base.models.rtb.User#ADAPTER")
    public final User user;

    public SdkConfigRequest(App app, Device device, User user) {
        this(app, device, user, ByteString.EMPTY);
    }

    public SdkConfigRequest(App app, Device device, User user, ByteString unknownFields) {
        super(ADAPTER, unknownFields);
        this.app = app;
        this.device = device;
        this.user = user;
    }

    @Override
    public Builder newBuilder() {
        Builder builder = new Builder();
        builder.app = app;
        builder.device = device;
        builder.user = user;
        builder.addUnknownFields(unknownFields());
        return builder;
    }

    @Override
    public boolean equals(Object other) {
        if (other == this) return true;
        if (!(other instanceof SdkConfigRequest)) return false;
        SdkConfigRequest o = (SdkConfigRequest) other;
        return unknownFields().equals(o.unknownFields()) && Internal.equals(app, o.app) && Internal.equals(device, o.device) && Internal.equals(user, o.user);
    }

    @Override
    public int hashCode() {
        int result = super.hashCode;
        if (result == 0) {
            result = unknownFields().hashCode();
            result = result * 37 + (app != null ? app.hashCode() : 0);
            result = result * 37 + (device != null ? device.hashCode() : 0);
            result = result * 37 + (user != null ? user.hashCode() : 0);
            super.hashCode = result;
        }
        return result;
    }

    @Override
    public String toString() {
        StringBuilder builder = new StringBuilder();
        if (app != null) builder.append(", app=").append(app);
        if (device != null) builder.append(", device=").append(device);
        if (user != null) builder.append(", user=").append(user);
        return builder.replace(0, 2, "SdkConfigRequest{").append('}').toString();
    }

    public static final class Builder extends Message.Builder<SdkConfigRequest, Builder> {
        public App app;

        public Device device;

        public User user;

        public Builder() {
        }

        /**
         * 必填！应用信息
         */
        public Builder app(App app) {
            this.app = app;
            return this;
        }

        /**
         * 必填！设备信息
         */
        public Builder device(Device device) {
            this.device = device;
            return this;
        }

        /**
         * 用户信息
         */
        public Builder user(User user) {
            this.user = user;
            return this;
        }

        @Override
        public SdkConfigRequest build() {
            return new SdkConfigRequest(app, device, user, super.buildUnknownFields());
        }
    }

    private static final class ProtoAdapter_SdkConfigRequest extends ProtoAdapter<SdkConfigRequest> {
        public ProtoAdapter_SdkConfigRequest() {
            super(FieldEncoding.LENGTH_DELIMITED, SdkConfigRequest.class);
        }

        @Override
        public int encodedSize(SdkConfigRequest value) {
            return App.ADAPTER.encodedSizeWithTag(1, value.app) + Device.ADAPTER.encodedSizeWithTag(2, value.device) + User.ADAPTER.encodedSizeWithTag(5, value.user) + value.unknownFields().size();
        }

        @Override
        public void encode(ProtoWriter writer, SdkConfigRequest value) throws IOException {
            App.ADAPTER.encodeWithTag(writer, 1, value.app);
            Device.ADAPTER.encodeWithTag(writer, 2, value.device);
            User.ADAPTER.encodeWithTag(writer, 5, value.user);
            writer.writeBytes(value.unknownFields());
        }

        @Override
        public SdkConfigRequest decode(ProtoReader reader) throws IOException {
            Builder builder = new Builder();
            long token = reader.beginMessage();
            for (int tag; (tag = reader.nextTag()) != -1; ) {
                switch (tag) {
                    case 1:
                        builder.app(App.ADAPTER.decode(reader));
                        break;
                    case 2:
                        builder.device(Device.ADAPTER.decode(reader));
                        break;
                    case 5:
                        builder.user(User.ADAPTER.decode(reader));
                        break;
                    default: {
                        FieldEncoding fieldEncoding = reader.peekFieldEncoding();
                        Object value = fieldEncoding.rawProtoAdapter().decode(reader);
                        builder.addUnknownField(tag, fieldEncoding, value);
                    }
                }
            }
            reader.endMessage(token);
            return builder.build();
        }

        @Override
        public SdkConfigRequest redact(SdkConfigRequest value) {
            Builder builder = value.newBuilder();
            if (builder.app != null) builder.app = App.ADAPTER.redact(builder.app);
            if (builder.device != null) builder.device = Device.ADAPTER.redact(builder.device);
            if (builder.user != null) builder.user = User.ADAPTER.redact(builder.user);
            builder.clearUnknownFields();
            return builder.build();
        }
    }
}
