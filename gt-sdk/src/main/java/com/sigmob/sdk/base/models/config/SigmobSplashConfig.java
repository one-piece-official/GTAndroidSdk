// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: sigmob_ssp_config.proto
package com.sigmob.sdk.base.models.config;

import android.os.Parcelable;

import com.czhj.wire.AndroidMessage;
import com.czhj.wire.FieldEncoding;
import com.czhj.wire.Message;
import com.czhj.wire.ProtoAdapter;
import com.czhj.wire.ProtoReader;
import com.czhj.wire.ProtoWriter;
import com.czhj.wire.WireField;
import com.czhj.wire.internal.Internal;
import com.czhj.wire.okio.ByteString;

import java.io.IOException;

/**
 * 开屏配置
 */
public final class SigmobSplashConfig extends AndroidMessage<SigmobSplashConfig, SigmobSplashConfig.Builder> {
    public static final ProtoAdapter<SigmobSplashConfig> ADAPTER = new ProtoAdapter_SigmobSplashConfig();

    public static final Parcelable.Creator<SigmobSplashConfig> CREATOR = AndroidMessage.newCreator(ADAPTER);

    private static final long serialVersionUID = 0L;

    public static final Integer DEFAULT_CACHETOP = 0;

    public static final Integer DEFAULT_MATERIAL_EXPIRED_TIME = 0;

    /**
     * 开屏素材缓存上限，小于0，则清楚缓存
     */
    @WireField(
            tag = 1,
            adapter = "com.squareup.wire.ProtoAdapter#INT32"
    )
    public final Integer cacheTop;

    /**
     * 素材的过期时间，单位天数，若小于0，则清楚缓存
     */
    @WireField(
            tag = 2,
            adapter = "com.squareup.wire.ProtoAdapter#INT32"
    )
    public final Integer material_expired_time;

    public SigmobSplashConfig(Integer cacheTop, Integer material_expired_time) {
        this(cacheTop, material_expired_time, ByteString.EMPTY);
    }

    public SigmobSplashConfig(Integer cacheTop, Integer material_expired_time,
                              ByteString unknownFields) {
        super(ADAPTER, unknownFields);
        this.cacheTop = cacheTop;
        this.material_expired_time = material_expired_time;
    }

    @Override
    public Builder newBuilder() {
        Builder builder = new Builder();
        builder.cacheTop = cacheTop;
        builder.material_expired_time = material_expired_time;
        builder.addUnknownFields(unknownFields());
        return builder;
    }

    @Override
    public boolean equals(Object other) {
        if (other == this) return true;
        if (!(other instanceof SigmobSplashConfig)) return false;
        SigmobSplashConfig o = (SigmobSplashConfig) other;
        return unknownFields().equals(o.unknownFields())
                && Internal.equals(cacheTop, o.cacheTop)
                && Internal.equals(material_expired_time, o.material_expired_time);
    }

    @Override
    public int hashCode() {
        int result = super.hashCode;
        if (result == 0) {
            result = unknownFields().hashCode();
            result = result * 37 + (cacheTop != null ? cacheTop.hashCode() : 0);
            result = result * 37 + (material_expired_time != null ? material_expired_time.hashCode() : 0);
            super.hashCode = result;
        }
        return result;
    }

    @Override
    public String toString() {
        StringBuilder builder = new StringBuilder();
        if (cacheTop != null) builder.append(", cacheTop=").append(cacheTop);
        if (material_expired_time != null)
            builder.append(", material_expired_time=").append(material_expired_time);
        return builder.replace(0, 2, "SigmobSplashConfig{").append('}').toString();
    }

    public static final class Builder extends Message.Builder<SigmobSplashConfig, Builder> {
        public Integer cacheTop;

        public Integer material_expired_time;

        public Builder() {
        }

        /**
         * 开屏素材缓存上限，小于0，则清楚缓存
         */
        public Builder cacheTop(Integer cacheTop) {
            this.cacheTop = cacheTop;
            return this;
        }

        /**
         * 素材的过期时间，单位天数，若小于0，则清楚缓存
         */
        public Builder material_expired_time(Integer material_expired_time) {
            this.material_expired_time = material_expired_time;
            return this;
        }

        @Override
        public SigmobSplashConfig build() {
            return new SigmobSplashConfig(cacheTop, material_expired_time, super.buildUnknownFields());
        }
    }

    private static final class ProtoAdapter_SigmobSplashConfig extends ProtoAdapter<SigmobSplashConfig> {
        public ProtoAdapter_SigmobSplashConfig() {
            super(FieldEncoding.LENGTH_DELIMITED, SigmobSplashConfig.class);
        }

        @Override
        public int encodedSize(SigmobSplashConfig value) {
            return ProtoAdapter.INT32.encodedSizeWithTag(1, value.cacheTop)
                    + ProtoAdapter.INT32.encodedSizeWithTag(2, value.material_expired_time)
                    + value.unknownFields().size();
        }

        @Override
        public void encode(ProtoWriter writer, SigmobSplashConfig value) throws IOException {
            ProtoAdapter.INT32.encodeWithTag(writer, 1, value.cacheTop);
            ProtoAdapter.INT32.encodeWithTag(writer, 2, value.material_expired_time);
            writer.writeBytes(value.unknownFields());
        }

        @Override
        public SigmobSplashConfig decode(ProtoReader reader) throws IOException {
            Builder builder = new Builder();
            long token = reader.beginMessage();
            for (int tag; (tag = reader.nextTag()) != -1; ) {
                switch (tag) {
                    case 1:
                        builder.cacheTop(ProtoAdapter.INT32.decode(reader));
                        break;
                    case 2:
                        builder.material_expired_time(ProtoAdapter.INT32.decode(reader));
                        break;
                    default: {
                        FieldEncoding fieldEncoding = reader.peekFieldEncoding();
                        Object value = fieldEncoding.rawProtoAdapter().decode(reader);
                        builder.addUnknownField(tag, fieldEncoding, value);
                    }
                }
            }
            reader.endMessage(token);
            return builder.build();
        }

        @Override
        public SigmobSplashConfig redact(SigmobSplashConfig value) {
            Builder builder = value.newBuilder();
            builder.clearUnknownFields();
            return builder.build();
        }
    }
}
